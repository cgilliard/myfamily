CC=gcc
CC_FLAGS=-I..
SOURCES := $(patsubst %.c,%.o,$(wildcard *.c))
HEADERS := $(wildcard *.h) $(wildcard parser/*.h) $(wildcard util/*.h)

%.o : %.c $(HEADERS)
	$(CC) $(CC_FLAGS) -c -o $@ $<
all: $(OBJS)
clean:
	rm -rf *.o
